pipeline {
    agent any
    tools {
        maven 'Maven 3.3.9'
        jdk 'jdk8'
    }
    stage('Build') { 
            steps { 
               echo 'This is a minimal pipeline.' 
            }
        }
}
