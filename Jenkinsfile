pipeline {
    agent any
    tools {
        maven 'Maven 3.3.9'
        jdk 'local_jdk'
    }
    stages{
        stage('Build') { 
                steps { 
                   echo 'This is a minimal pipeline.' 
                }
            }
    }
}
