pipeline {
    agent any
    tools {
        maven 'local_maven'
        jdk 'local_jdk'
    }
    stages{
        stage('Build') { 
                steps { 
                   echo 'This is a minimal pipeline.' 
                }
            }
    }
}
